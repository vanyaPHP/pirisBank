<html>
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    </head>
    <body>
        {{ include('navbar.html.twig') }}

        <h2 style="text-align: center;margin-top: 50px">Заполните форму для заключения договора</h2>

        <form method="POST" style="margin-top: 100px;margin-left: 620px" action="http://localhost:8000/deposits/store">
            <label>Клиент</label>
            <select name="clientId" class="form-select" style="width: 50%">
                {% for client in clients %}
                    <option value="{{ client.id }}">
                        {{ client.firstName }} {{ client.lastName }} ({{ client.passportId }})
                    </option>
                {% endfor %}
            </select>

            <br>

            <label>Депозитный план</label>
            <select name="depositPlanId" class="form-select" style="width: 50%">
                {% for depositPlan in depositPlans %}
                    <option value="{{ depositPlan.id }}">
                        {{ depositPlan.name }}  ({{ depositPlan.monthPeriod }} месяца
                        под {{ depositPlan.percent }} процента,
                        минимальная сумма {{ depositPlan.minAmount}} бел. рублей)
                    </option>
                {% endfor %}
            </select>

            <br>

            <label>Сумма</label>
            <input id="amount" name="amount" style="width: 50%" class="form-control">

            <br>

            <input type="submit" class="btn btn-primary" value="Сохранить">
        </form>
    </body>
</html>